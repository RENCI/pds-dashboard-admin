version: '3.7'

services:
  pds-dashboard-admin:
    container_name: pds-dashboard-admin
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 8081:8081
