version: '3.7'

services:
  pds-dashboard-admin:
    container_name: default
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 8081:3000
    stdin_open: true
networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${IPAM_CONFIG_SUBNET_ADMIN_DASHBOARD}